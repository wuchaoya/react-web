import styled from 'styled-components';

export default styled.div`
  width: 8.94rem;
  height: 16rem;
  background-image: url(${(props) => props.imgUrl});
  background-size: 8.94rem 16rem;
  overflow:hidden;
  `;
